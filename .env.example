# ─── Dashboard API Configuration ────────────────────────────────────────
# The base URL for the backend API (without trailing slash).
# The dashboard API client will append /api/v1/ to this.
# Swagger docs: https://d-api.mindplayer.com/docs/
NEXT_PUBLIC_API_BASE_URL=https://d-api.mindplayer.com
